# called by php to clean sentinel and re-create sandbox
cd $HOME/svn/Sentinel
cabal clean  -- clean probably not necessary since we use cabal install rather than cabal build
rm -rf .cabal-sandbox
cabal sandbox init
