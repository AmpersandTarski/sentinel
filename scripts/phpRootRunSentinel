echo Running phpRootRunSentinel
# script that is called by the www server (from php)


killall --quiet sentinel
killall --quiet phpSentinelRunSentinel
# probably enough to kill phpSentinelRunSentinel, but killing both doesn't harm

sudo -u "sentinel /home/sentinel/svn/Sentinel/scripts/phpSentinelRunSentinel &"
