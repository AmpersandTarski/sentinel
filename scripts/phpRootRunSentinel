echo Running phpRootRunSentinel
# wrapper script that is called by the www server as root (from php)
# and calls sentinel as user 'sentinel'

sudo -u sentinel /home/sentinel/svn/Sentinel/scripts/runSentinel
